from setuptools import setup, find_packages

setup(
    name='gpiocontrol',
    version='1.0',
    description="Simple wrapper bash' GPIO pins commands",
    author='s17kf',
    packages=find_packages(),
)
